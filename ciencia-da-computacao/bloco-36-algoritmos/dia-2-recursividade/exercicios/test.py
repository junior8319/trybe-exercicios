while True:
    x = list(map(int, input("Entre com uma lista de números: ").split()))
    print(x)

# Este teste me serviu para entender como receber de entrada de usuário uma lista.
# Ao teclar 'enter' no terminal, o programa dá como finalizada a tarefa
# e imprime (linha 3) a lista que foi digitada.
